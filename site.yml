- hosts: web_servers
  gather_facts: no        # skip fact gathering to avoid hangs
  tasks:
    - name: Test connectivity to web servers
      ansible.builtin.ping:
      become: false        # ping doesn’t require sudo

    - name: Ensure Nginx is installed
      ansible.builtin.package:
        name: nginx
        state: present
      become: true         # sudo required for package installation
